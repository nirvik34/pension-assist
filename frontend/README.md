# SAMAAN Pension Assist — Frontend (Next.js)

Minimal Next.js (App Router) scaffold for the SAMAAN PWA.

Quick start:

```bash
cd frontend
npm install
npm run dev
```

Notes:
- Set `NEXT_PUBLIC_API_URL` to point to the backend (default `http://localhost:8000`).
- PWA support via `next-pwa`.
- Accessible, elder-friendly UI components in `components/`.
